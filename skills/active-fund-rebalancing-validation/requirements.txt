# 核心依赖（必需）
pandas>=1.5.0
requests>=2.28.0
beautifulsoup4>=4.11.0
lxml>=4.9.0

# 行情数据增强（可选，未安装时脚本会自动降级）
akshare>=1.10.0

# PDF 生成（二选一即可）
# 方案 A：WeasyPrint（Linux/macOS 需系统库 pango/cairo/gobject）
weasyprint>=60.0
markdown>=3.4.0

# 方案 B：Playwright + Chromium（macOS 推荐）
# playwright>=1.40.0
# 安装后需执行：playwright install chromium
